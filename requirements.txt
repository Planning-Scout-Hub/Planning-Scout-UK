# PlanningScout — Python dependencies
# Install locally:  pip install -r requirements.txt
# GitHub Actions installs this automatically before every run

requests>=2.31.0
beautifulsoup4>=4.12.0
pdfplumber>=0.10.0
gspread>=6.0.0
google-auth>=2.27.0
urllib3>=2.0.0
